const accent = {
    buttonPrimary: "#f00"
}
const sizes = {
    buttonNormal: '6px 20px',
    borderRadius: '20px',
}
export const MainTheme = {
    colors: {        
        accent: accent,
        background: "#eee",
        text: "#212121",
        textButton: "#fff",
    },
    sizes: sizes,
}

const theme = MainTheme;
export default theme;